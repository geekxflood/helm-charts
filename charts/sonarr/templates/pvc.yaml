---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config-iscsi-pvc
  labels:
    app: sonarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology-csi-iscsi-retain
  resources:
    requests:
      storage: 50Gi
