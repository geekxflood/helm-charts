apiVersion: v1
kind: Service
metadata:
  name: {{ include "kapowarr.fullname" . }}
  labels:
    {{- include "kapowarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kapowarr.selectorLabels" . | nindent 4 }}
