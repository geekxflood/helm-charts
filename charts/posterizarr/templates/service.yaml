apiVersion: v1
kind: Service
metadata:
  name: {{ include "posterizarr.fullname" . }}
  labels:
    {{- include "posterizarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "posterizarr.selectorLabels" . | nindent 4 }}
