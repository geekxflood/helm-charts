Thank you for installing {{ .Chart.Name }}!

Your Posterizarr deployment is ready.

1. Get the application URL:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "posterizarr.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  echo "Visit http://127.0.0.1:8000 to access Posterizarr Web UI"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8000:8000
{{- end }}

2. Configure Posterizarr:
   - Access the Web UI at the URL above
   - Update /config/config.json with your API keys
   - Configure your media server connections (Plex/Jellyfin/Emby)

3. Documentation:
   - Configuration Guide: https://fscorrupt.github.io/posterizarr/configuration/
   - GitHub: https://github.com/fscorrupt/Posterizarr

Storage mounts:
  - Config: /config ({{ if .Values.persistence.config.enabled }}PVC enabled{{ else }}ephemeral{{ end }})
  - Assets: /assets ({{ if .Values.persistence.assets.enabled }}PVC enabled{{ else }}ephemeral{{ end }})
  - Assets Backup: /assetsbackup ({{ if .Values.persistence.assetsbackup.enabled }}PVC enabled{{ else }}ephemeral{{ end }})
  - Manual Assets: /manualassets ({{ if .Values.persistence.manualassets.enabled }}PVC enabled{{ else }}ephemeral{{ end }})
