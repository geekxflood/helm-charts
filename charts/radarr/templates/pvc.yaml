---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config-iscsi-pvc
  labels:
    app: radarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology-csi-iscsi-retain
  resources:
    requests:
      storage: 50Gi
