apiVersion: v1
kind: Service
metadata:
  name: {{ include "jellyfin-vue.fullname" . }}
  labels:
    {{- include "jellyfin-vue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jellyfin-vue.selectorLabels" . | nindent 4 }}
