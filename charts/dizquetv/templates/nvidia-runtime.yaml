{{- if .Values.runtime.nvidia.enabled }}
---
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ include "dizquetv.fullname" . }}
  labels:
    {{- include "dizquetv.labels" . | nindent 4 }}
handler: nvidia
{{- end }}
