apiVersion: v1
kind: Service
metadata:
  name: {{ include "overseer.fullname" . }}
  labels:
    {{- include "overseer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "overseer.selectorLabels" . | nindent 4 }}
